/**
 * Automation client facade — Patchright (undetected Playwright fork) for Chromium CDP.
 * Types come from Patchright; keep this as the only import site for browser runtime APIs.
 */
export { chromium } from "patchright";
export type { Browser, BrowserContext, Page, Response } from "patchright";
